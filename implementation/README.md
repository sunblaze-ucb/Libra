Instructions to run the tests:

###Lanczos
`python build.py`
`python run.py`

use `sudo` if necessary.

###Matmul
`python build.py`
`python run.py`

use `sudo` if necessary.

###SHA256
`python build.py`
`python run.py`

use `sudo` if necessary.